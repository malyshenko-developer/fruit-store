services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: apple_store
      POSTGRES_PASSWORD: dev_password
      POSTGRES_DB: apple_store
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  pg_data:
  redis_data: